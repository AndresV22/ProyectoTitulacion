library('irace')
escenario <- readScenario(filename = "scenario.txt", scenario = defaultScenario())
parametros <- readParameters(file = "parameters.txt")
irace(scenario = escenario, parameters = parametros)
